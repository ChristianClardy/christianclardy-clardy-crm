// This file was used by the Base44 SDK and is no longer needed.
// Kept as a stub so any remaining imports don't break during migration.
export const appParams = {
  appId: null,
  token: null,
  functionsVersion: null,
  appBaseUrl: null,
  fromUrl: typeof window !== 'undefined' ? window.location.href : '/',
};
